# Experiment 2: Card-Based Layout Using Bootstrap (React + Vite)

This experiment demonstrates how to build a **responsive card-based layout** using **Bootstrap 5** in a **React application** created with **Vite**.  
The UI dynamically renders multiple cards using an array and the `map()` function.

---

## 🎯 Aim of the Experiment

To design and implement a **card-based responsive layout** using Bootstrap components in React, and to understand:
- Bootstrap grid system
- Card component
- Dynamic rendering using `map()`

---

## 🧪 Experiment Description

- Three cards are displayed on the screen.
- Each card contains:
  - A title
  - A description
  - A “Learn More” button
- Cards are rendered dynamically from an array of objects.
- Bootstrap is used for styling and responsiveness.

---

## ✨ Features

- React Functional Component
- Dynamic UI rendering using `map()`
- Bootstrap 5 Card component
- Responsive grid layout
- Clean and centered design
- Shadow effect for better UI appearance

---

## 🛠️ Tech Stack

- **React**
- **Vite**
- **Bootstrap 5**
- **JavaScript (ES6)**
- **CSS**

---

## 📦 Installation & Setup

### 1️⃣ Create Project Using Vite
```bash
npm create vite@latest experiment-2
cd experiment-2
npm install
